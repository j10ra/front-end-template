(function($, window, document) {
    'use strict';

    $(function() {
        if (window.PIE) {
            // $('.rounded').each(function() {
            //     PIE.attach(this);
            // });
        }
    });

}(jQuery, window, document));